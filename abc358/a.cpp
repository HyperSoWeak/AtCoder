#include <bits/stdc++.h>
#define rep(i,a,b) for(int i=a; i<b; ++i)
#define rep1(i,a,b) for(int i=a; i<=b; ++i)
#define int long long
using namespace std;

signed main() {
    string a, b;
    cin >> a >> b;
    if(a == "AtCoder" && b == "Land") cout << "Yes";
    else cout << "No";
}